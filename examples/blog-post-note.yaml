name: BlogPostNote
routed: false

fields:
  - name: body
    type: string
  - name: rating
    type: int
    optional: true
  - name: createdAt
    type: datetime

sorts:
  - createdAt

filters:
  - body

pagination:
  limits: [5, 10]
  default: 5
